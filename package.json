{
  "name": "polaris",
  "private": true,
  "packageManager": "npm@11.19.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:seed": "tsx --env-file=.env.local supabase/seed.ts"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "tsx": "^4.19.0"
  },
  "overrides": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  }
}
